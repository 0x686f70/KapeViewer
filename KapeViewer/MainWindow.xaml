<Window x:Class="KapeViewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:KapeViewer"
        xmlns:models="clr-namespace:KapeViewer.Models"
        xmlns:vm="clr-namespace:KapeViewer.ViewModels"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="KAPE Viewer" Height="800" Width="1200"
        d:DataContext="{d:DesignInstance Type=vm:MainViewModel}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}">
    
    <Window.InputBindings>
        <KeyBinding Key="O" Modifiers="Control" Command="{Binding FileTree.OpenCaseCommand}"/>
        <KeyBinding Key="F5" Command="{Binding FileTree.OpenCaseCommand}"/> <!-- Reusing OpenCase for Refresh for now -->
        <KeyBinding Key="T" Modifiers="Control" Command="{Binding BuildTimelineCommand}"/>
        <KeyBinding Key="E" Modifiers="Control" Command="{Binding Table.ExportTableCommand}"/>
    </Window.InputBindings>

    <DockPanel>
        <!-- Menu Bar -->
        <Menu DockPanel.Dock="Top" IsMainMenu="True">
            <MenuItem Header="_File">
                <MenuItem Header="_Open Case" Command="{Binding FileTree.OpenCaseCommand}" InputGestureText="Ctrl+O">
                    <MenuItem.Icon>
                        <materialDesign:PackIcon Kind="FolderOpen" />
                    </MenuItem.Icon>
                </MenuItem>
                <MenuItem Header="_Export Current Table" Command="{Binding Table.ExportTableCommand}" InputGestureText="Ctrl+E">
                    <MenuItem.Icon>
                        <materialDesign:PackIcon Kind="FileExport" />
                    </MenuItem.Icon>
                </MenuItem>
                <Separator/>
                <MenuItem Header="E_xit" Command="{Binding ExitCommand}">
                    <MenuItem.Icon>
                        <materialDesign:PackIcon Kind="ExitToApp" />
                    </MenuItem.Icon>
                </MenuItem>
            </MenuItem>
            <MenuItem Header="_Tools">
                <MenuItem Header="Build Global _Timeline" Command="{Binding BuildTimelineCommand}" InputGestureText="Ctrl+T">
                    <MenuItem.Icon>
                        <materialDesign:PackIcon Kind="TimelineClock" />
                    </MenuItem.Icon>
                </MenuItem>
                <MenuItem Header="_Export Timeline" Command="{Binding Timeline.ExportTimelineCommand}">
                    <MenuItem.Icon>
                        <materialDesign:PackIcon Kind="FileExport" />
                    </MenuItem.Icon>
                </MenuItem>
            </MenuItem>
            <MenuItem Header="_View">
                <MenuItem Header="_UTC/Local Time" IsCheckable="True" IsChecked="{Binding Timeline.IsUtcMode, Mode=TwoWay}" Command="{Binding Timeline.ToggleUtcModeCommand}"/>
            </MenuItem>
        </Menu>

        <!-- Toolbar -->
        <materialDesign:ColorZone Mode="PrimaryMid" Padding="8" DockPanel.Dock="Top">
            <DockPanel>
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                    <Button Command="{Binding FileTree.OpenCaseCommand}" ToolTip="Open Case (Ctrl+O)" Style="{StaticResource MaterialDesignToolButton}" Foreground="{DynamicResource MaterialDesignBody}">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="FolderOpen" />
                            <TextBlock Text="Open Case" Margin="8 0 0 0" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                    <Button Command="{Binding BuildTimelineCommand}" ToolTip="Build Timeline (Ctrl+T)" Style="{StaticResource MaterialDesignToolButton}" Foreground="{DynamicResource MaterialDesignBody}">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="TimelineClock" />
                            <TextBlock Text="Build Timeline" Margin="8 0 0 0" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                </StackPanel>

                <StackPanel Orientation="Horizontal" DockPanel.Dock="Right" HorizontalAlignment="Right">
                    <!-- Filters -->
                    <DatePicker Width="100" materialDesign:HintAssist.Hint="From" SelectedDate="{Binding Timeline.FilterCriteria.FromDate}" Margin="0 0 8 0" VerticalAlignment="Center"/>
                    <DatePicker Width="100" materialDesign:HintAssist.Hint="To" SelectedDate="{Binding Timeline.FilterCriteria.ToDate}" Margin="0 0 8 0" VerticalAlignment="Center"/>
                    
                    <ToggleButton Style="{StaticResource MaterialDesignSwitchToggleButton}" IsChecked="{Binding Timeline.IsUtcMode}" Command="{Binding Timeline.ToggleUtcModeCommand}" ToolTip="UTC/Local" Margin="0 0 8 0"/>
                    <TextBlock Text="UTC" VerticalAlignment="Center" Margin="0 0 16 0"/>

                    <!-- Search -->
                    <TextBox Width="200" materialDesign:HintAssist.Hint="Search..." Text="{Binding Timeline.FilterCriteria.SearchText, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center" Margin="0 0 8 0">
                        <TextBox.InputBindings>
                            <KeyBinding Key="Enter" Command="{Binding Timeline.ApplyFiltersCommand}"/>
                        </TextBox.InputBindings>
                    </TextBox>
                    <Button Command="{Binding Timeline.ApplyFiltersCommand}" Style="{StaticResource MaterialDesignToolButton}" Foreground="{DynamicResource MaterialDesignBody}">
                        <materialDesign:PackIcon Kind="Magnify" />
                    </Button>
                </StackPanel>
            </DockPanel>
        </materialDesign:ColorZone>

        <!-- Status Bar -->
        <StatusBar DockPanel.Dock="Bottom" Background="{DynamicResource MaterialDesignPaper}">
            <StatusBarItem>
                <TextBlock Text="{Binding FileTree.CurrentCasePath}" />
            </StatusBarItem>
            <Separator Style="{StaticResource MaterialDesignStatusBarSeparator}" />
            <StatusBarItem>
                <TextBlock Text="{Binding Table.StatusMessage}" />
            </StatusBarItem>
            <Separator Style="{StaticResource MaterialDesignStatusBarSeparator}" />
            <StatusBarItem>
                <TextBlock Text="{Binding Timeline.StatusMessage}" />
            </StatusBarItem>
        </StatusBar>

        <!-- Main Content -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="280"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Left Panel: File Tree -->
            <materialDesign:Card Grid.Column="0" Margin="8" VerticalAlignment="Stretch">
                <TreeView ItemsSource="{Binding FileTree.Groups}" x:Name="FileTreeView">
                    <TreeView.Resources>
                        <HierarchicalDataTemplate DataType="{x:Type models:GroupNode}" ItemsSource="{Binding Files}">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="Folder" Margin="0 0 8 0" Foreground="{DynamicResource PrimaryHueMidBrush}"/>
                                <TextBlock Text="{Binding Name}" FontWeight="Bold"/>
                                <TextBlock Text=" (" Foreground="Gray"/>
                                <TextBlock Text="{Binding FileCount}" Foreground="Gray"/>
                                <TextBlock Text=")" Foreground="Gray"/>
                            </StackPanel>
                        </HierarchicalDataTemplate>
                        <DataTemplate DataType="{x:Type models:CsvFileItem}">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="FileDelimited" Margin="0 0 8 0"/>
                                <TextBlock Text="{Binding DisplayName}"/>
                            </StackPanel>
                        </DataTemplate>
                    </TreeView.Resources>
                    <TreeView.ItemContainerStyle>
                        <Style TargetType="{x:Type TreeViewItem}" BasedOn="{StaticResource MaterialDesignTreeViewItem}">
                            <Setter Property="IsSelected" Value="{Binding IsSelected, Mode=TwoWay}" />
                            <EventSetter Event="Selected" Handler="TreeViewItem_Selected"/>
                        </Style>
                    </TreeView.ItemContainerStyle>
                </TreeView>
            </materialDesign:Card>

            <GridSplitter Grid.Column="1" Width="4" HorizontalAlignment="Center" VerticalAlignment="Stretch" Background="Transparent"/>

            <!-- Right Panel: Tabs -->
            <materialDesign:Card Grid.Column="2" Margin="8" VerticalAlignment="Stretch">
                <TabControl SelectedIndex="{Binding SelectedTabIndex}" Style="{StaticResource MaterialDesignTabControl}">
                    <TabItem Header="Table">
                        <DataGrid ItemsSource="{Binding Table.CurrentTable}"
                                  AutoGenerateColumns="True"
                                  IsReadOnly="True"
                                  HeadersVisibility="All"
                                  GridLinesVisibility="All"
                                  CanUserAddRows="False"
                                  EnableRowVirtualization="True"
                                  EnableColumnVirtualization="True"
                                  VirtualizingPanel.IsVirtualizing="True"
                                  VirtualizingPanel.VirtualizationMode="Recycling"
                                  Style="{StaticResource MaterialDesignDataGrid}"
                                  CellStyle="{StaticResource MaterialDesignDataGridCell}"
                                  ColumnHeaderStyle="{StaticResource MaterialDesignDataGridColumnHeader}"
                                  RowStyle="{StaticResource MaterialDesignDataGridRow}"/>
                    </TabItem>
                    <TabItem Header="Timeline">
                        <ListView ItemsSource="{Binding Timeline.FilteredEvents}"
                                  VirtualizingPanel.IsVirtualizing="True"
                                  VirtualizingPanel.VirtualizationMode="Recycling"
                                  Style="{StaticResource MaterialDesignListView}">
                            <ListView.View>
                                <GridView ColumnHeaderContainerStyle="{StaticResource MaterialDesignGridViewColumnHeader}">
                                    <GridViewColumn Header="Timestamp" Width="180" DisplayMemberBinding="{Binding Timestamp, StringFormat='yyyy-MM-dd HH:mm:ss.fff'}"/>
                                    <GridViewColumn Header="Source" Width="200" DisplayMemberBinding="{Binding Source}"/>
                                    <GridViewColumn Header="Group" Width="150" DisplayMemberBinding="{Binding GroupName}"/>
                                    <GridViewColumn Header="Description" Width="600" DisplayMemberBinding="{Binding Description}"/>
                                </GridView>
                            </ListView.View>
                        </ListView>
                    </TabItem>
                </TabControl>
            </materialDesign:Card>

            <!-- Loading Overlay -->
            <Grid Grid.ColumnSpan="3" 
                  Background="#80000000" 
                  Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}"
                  d:Visibility="Visible">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <ProgressBar Style="{StaticResource MaterialDesignCircularProgressBar}"
                                 Value="0"
                                 IsIndeterminate="True"
                                 Width="50"
                                 Height="50"/>
                    <TextBlock Text="Processing..." 
                               Foreground="White" 
                               Margin="0 10 0 0" 
                               HorizontalAlignment="Center"
                               FontSize="16"/>
                </StackPanel>
            </Grid>
        </Grid>
    </DockPanel>
</Window>
